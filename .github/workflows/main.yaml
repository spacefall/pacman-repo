name: Update Packages
on:
  push:
    branches: [ "patches" ]
  pull_request:
    branches: [ "patches" ]
  workflow_dispatch:

jobs:
  vscodium:
    uses: ./.github/workflows/builder.yml
    with:
      package: vscodium-electron

  discord:
    uses: ./.github/workflows/builder.yml
    with:
      package: discord-electron

      
